# The dsh-model-map bundle patch: mount the host half next to the dsh-base
# layer. The client half is served through the dsh.client declaration in
# package.json once this row is active.
- insert:
    - id: dsh-model-map
      name: dsh-model-map
